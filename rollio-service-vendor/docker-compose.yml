version: '2'

services:
  app-service-vendor:
    container_name: rollio-service-vendor
    build: .
    env_file:
      - .env
    environment:
      - NODE_ENV=DEVELOPMENT_DOCKER
    restart: always
    ports: 
      - '80:3001'
    links:
      - redis
      - mongo
      - rabbitmq